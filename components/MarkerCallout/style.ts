import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  callout: {
    padding: 10,
    borderRadius: 6,
    minWidth: 150,
  },
  calloutTitle: {
    fontWeight: 'bold',
    marginBottom: 5,
  },
  calloutText: {
    color: '#666',
  },
});
export default styles;
